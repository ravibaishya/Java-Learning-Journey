# Day 05 Practice Questions

## Concepts Covered in Today's Session

- `Integer.parseInt()`
- Converting `String` to `int`
- Arithmetic using command-line arguments
- `java.lang`
- JDK, JRE and JVM
- JAR (Java Archive)
- Decompilation
- Java Identifiers
- Reserved Keywords
- Naming Conventions

---

## Short Questions

1. Why do command-line numbers arrive as `String`?
2. What does `Integer.parseInt()` do?
3. What is the return type of `Integer.parseInt()`?
4. Why can't arithmetic be performed directly on String values?
5. What is `NumberFormatException`?
6. What is `java.lang`?
7. Why don't we import `String` or `System` manually?
8. What is the role of the JDK?
9. What is the role of the JRE?
10. What is the role of the JVM?
11. What is a JAR file?
12. What is decompilation?
13. What is a Java identifier?
14. Why is `public` not a valid variable name?
15. What is the difference between an identifier rule and a naming convention?

---

## Coding Practice

Create programs using `Integer.parseInt()`.

1. Add Two Numbers
2. Subtract Two Numbers
3. Multiply Two Numbers
4. Divide Two Numbers
5. Total and Difference
6. Average of Two Numbers
7. Rectangle Area
8. Simple Interest
9. Shopping Bill
10. Mini Calculator *(Optional Challenge)*

---

## Challenge Practice

Create programs that receive **3–4 numeric arguments** and perform calculations.

Examples:

- Salary Calculator
- Electricity Bill
- Grocery Bill
- Student Marks Total
- Restaurant Bill

---

## Concept Check

Answer without looking at notes.

- Why is `Integer.parseInt("20")` valid?
- Why does `Integer.parseInt("Hello")` fail?
- Explain the flow: `String → parseInt() → int → Calculation`.
- Explain the flow: `Source Code → Compile → JAR → Production`.

---

## Goal of Day 05

Understand how Java converts **text input into numeric data** and how Java applications move from development to execution.
