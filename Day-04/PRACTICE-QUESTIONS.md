# Day 04 Practice Questions

## Concepts Covered in Today's Session

- Java Program Execution Flow (`.java → .class → JVM`)
- `javac` vs `java` 
- Command-Line Arguments
- `String[] args`
- Array Indexing (`args[0]`, `args[1]`...)
- Program Arguments vs JVM Arguments
- Dynamic Input (Avoid Hardcoding)

---

## Short Questions

1. What are command-line arguments in Java?
2. Why are command-line arguments received as `String` values?
3. What is the purpose of `String[] args` in `main()`?
4. Why does array indexing start from `0`?
5. What is the difference between `args[0]` and `args[1]`?
6. What is hardcoding?
7. Why should we avoid hardcoding in real-world applications?
8. What is the difference between Program Arguments and JVM Arguments?
9. What is the purpose of `javac`?
10. What is the role of the JVM after compilation?

---

## Coding Practice

Create similar programs using **only `String[] args`**.

1. Personal Introduction
2. Student Details
3. Employee Profile
4. Product Information
5. Amazon Order Details
6. Bank Account Details
7. Credit Card Processing
8. Movie Ticket Booking
9. Food Delivery Order
10. Travel Booking

---

## Challenge Practice

Create programs that receive **4–5 command-line arguments** and print a formatted summary.

Examples:

- Courier Tracking
- Library Book Details
- Flight Booking
- Hospital Patient Details
- Event Registration

---

## Concept Check

Answer without looking at notes.

- What happens if you access `args[3]` when only two arguments are passed?
- Why must a `.java` file be compiled again after modification?
- How does command-line input make a program reusable?

---

## Goal of Day 04

Learn how to build programs that receive **dynamic input** instead of changing the source code every time.
